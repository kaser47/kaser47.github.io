@model RecentlyAddedShows.Service.Models.RecentlyAddedShowsViewModel
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 id="headline" class="display-4">Entertainment Tracker</h1>
</div>

<div class="container">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <ul class="nav nav-tabs card-header-tabs" id="bologna-list" role="tablist">
                        <li class="nav-item">
                            <a target="_blank" class="nav-link active" href="#errors" role="tab" aria-controls="errors" aria-selected="true">Errors</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#cartoons" role="tab" aria-controls="cartoons" aria-selected="false">Cartoons</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#anime" role="tab" aria-controls="anime" aria-selected="false">Anime</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#nextup" role="tab" aria-controls="nextup" aria-selected="false">TV - Next Up</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#tvpopular" role="tab" aria-controls="tvpopular" aria-selected="false">TV - Popular</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#tvcollection" role="tab" aria-controls="tvcollection" aria-selected="false">TV - Collection</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#recentlyupdated" role="tab" aria-controls="recentlyupdated" aria-selected="false">TV - Recently Updated</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#moviepopular" role="tab" aria-controls="moviepopular" aria-selected="false">Movie - Popular</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#moviefavourites" role="tab" aria-controls="moviefavourites" aria-selected="false">Movie - Watchlist</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#switch" role="tab" aria-controls="switch" aria-selected="false">Game - Switch</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#pc" role="tab" aria-controls="pc" aria-selected="false">Game - PC</a>
                        </li>
                        <li class="nav-item">
                            <a target="_blank" class="nav-link" href="#ps4" role="tab" aria-controls="ps4" aria-selected="false">Game - PS4</a>
                        </li>
                    </ul>
                </div>
                <div class="card-body">

                    <div class="tab-content mt-3">

                        <div class="tab-pane active" id="errors" role="tabpanel" aria-labelledby="deals-tab">
                            <button type="button" class="btn btn-danger" onclick="location.href='@Url.Action("Clear", "Home")'">Clear All</button>
                            <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("Refresh", "Home")'">Refresh</button>
                            <p />
                            <p />
                            <p />
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-2">Created</th>
                                    <th class="thead-dark col-3">Message</th>
                                    <th class="thead-dark col-4">StrackTrace</th>
                                </tr>
                                @foreach (var item in Model.Errors)
                                    {
                                <tr id="item">
                                    <td class="align-middle">
                                        <p>@item.Created</p>
                                    </td>
                                    <td class="align-middle">
                                        <p>@item.Message</p>
                                    </td>
                                    <td class="align-middle">
                                        <p>@item.StackTrace</p>
                                    </td>
                                </tr>
                                    }
                            </table>
                        </div>


                        <div class="tab-pane" id="cartoons" role="tabpanel">

                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.Cartoons)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>


                        </div>

                        <div class="tab-pane" id="anime" role="tabpanel" aria-labelledby="history-tab">

                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.Anime)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>

                        </div>

                        <div class="tab-pane" id="nextup" role="tabpanel" aria-labelledby="deals-tab">
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.NextUp)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>

                        </div>

                        <div class="tab-pane" id="tvpopular" role="tabpanel" aria-labelledby="deals-tab">
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.TVPopular)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>

                        </div>

                        <div class="tab-pane" id="tvcollection" role="tabpanel" aria-labelledby="deals-tab">
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.TVCollection)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>

                        </div>

                        <div class="tab-pane" id="moviepopular" role="tabpanel" aria-labelledby="deals-tab">
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.MoviePopular)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>


                        </div>

                        <div class="tab-pane" id="moviefavourites" role="tabpanel" aria-labelledby="deals-tab">
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.MovieWatchlist)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>


                        </div>

                        <div class="tab-pane" id="recentlyupdated" role="tabpanel" aria-labelledby="deals-tab">
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.RecentlyUpdated)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>


                        </div>

                        <div class="tab-pane" id="switch" role="tabpanel" aria-labelledby="deals-tab">
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.Switch)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>


                        </div>

                        <div class="tab-pane" id="pc" role="tabpanel" aria-labelledby="deals-tab">
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.PC)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>


                        </div>

                        <div class="tab-pane" id="ps4" role="tabpanel" aria-labelledby="deals-tab">
                            <table class="table table-striped table-bordered center">
                                <tr>
                                    <th class="thead-dark col-10">Name</th>
                                    @*<th class="thead-dark text-center col-2">Image</th>*@
                                </tr>
                                @foreach (var item in Model.PS4)
                                {
                                <tr id="item">
                                    <td class="align-middle">
                                        @if (item.IsUpdated)
                                            {
                                        <strong>
                                            <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                        </strong>
                                            }
                                            else
                                            {
                                        <a target="_blank" id="link" href="@item.Url">@item.Name</a>
                                            }
                                    </td>
                                    @*<td>
                        <div class="container">
                            <div class="px-0 center">
                                <a target="_blank" href="@item.Image" >
                                    <img src="@item.Image" class="img-thumbnail img-fluid">
                                </a>
                            </div>
                        </div>
                    </td>*@
                                </tr>
                                }
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="text-center">
    <p id="date">Last Updated - <span style="color: red">@Model.LastUpdated().ToString("dd/MM/yyyy")</span> @Model.TranslatedLastUpdated()</p>
</div>

<script>
    $(document).ready(function () {
        $('#bologna-list a').on('click', function (e) {
            e.preventDefault()
            $(this).tab('show')
        })
    });
    
</script>